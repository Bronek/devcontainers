{
    //  devcontainer.json is mapped to JSONC - hooray for comments !
    "name": "${devcontainerId}",
    "build": {
        "dockerfile": "Dockerfile",
        //  By default, GID and UID are only visible to shell and not exported.
        //  You need to add `export GID; export UID` somewhere, e.g. ~/.profile
        "args": {
            "USER_UID": "${localEnv:UID}",
            "USER_GID": "${localEnv:GID}",
            "USER_NAME": "${localEnv:USER}"
        }
    },
    "containerUser": "${localEnv:USER}",
    "containerEnv": {
        "DEVCONTAINER_ID": "${devcontainerId}",
        "WORKSPACE": "${localWorkspaceFolder}",
        // For git authentication, as per https://docs.docker.com/desktop/networking/#ssh-agent-forwarding
        "SSH_AUTH_SOCK": "/run/host-services/ssh-auth.sock"
    },
    "init": true,
    "customizations": {
        "vscode": {
            "extensions": [
                "llvm-vs-code-extensions.vscode-clangd",
                "xaver.clang-format",
                "ms-vscode.cpptools-themes",
                "eamodio.gitlens",
                "GitHub.vscode-pull-request-github",
                "jeff-hykin.better-cpp-syntax",
                "ajshort.include-autocomplete",
                "ms-azuretools.vscode-docker",
                "DavidAnson.vscode-markdownlint",
                "GitHub.remotehub",
                "GitHub.github-vscode-theme"
            ]
        }
    },
    "postCreateCommand": "sudo chown ${localEnv:USER}:${localEnv:USER} /run/host-services/ssh-auth.sock",
    //  This allows the `compile_commands.json` to be shared by both host and container
    "workspaceMount": "source=${localWorkspaceFolder},target=${localWorkspaceFolder},type=bind,consistency=cached",
    "workspaceFolder": "${localWorkspaceFolder}",
    "mounts": [
        {
            // For temporary build files etc.
            "source": "/tmp",
            "target": "/tmp",
            "type": "bind"
        },
        {
            // For git authentication, as per https://docs.docker.com/desktop/networking/#ssh-agent-forwarding
            "source": "/run/host-services/ssh-auth.sock",
            "target": "/run/host-services/ssh-auth.sock",
            "type": "bind"
        },
        {
            // For git commands
            "source": "${localEnv:HOME}/.gitconfig",
            "target": "/home/${localEnv:USER}/.gitconfig",
            "type": "bind"
        },
        {
            //  This allows sharing of conan data between containers
            "source": "conan_data",
            "target": "/home/${localEnv:USER}/.conan",
            "type": "volume"
        }
    ],
    // For debugging
    "capAdd": [
        "SYS_PTRACE"
    ],
    "securityOpt": [
        "seccomp=unconfined"
    ]
}